import { NotSameWachtwoordDirective } from './not-same-wachtwoord.directive';

describe('NotSameWachtwoordDirective', () => {
  it('should create an instance', () => {
    const directive = new NotSameWachtwoordDirective();
    expect(directive).toBeTruthy();
  });
});
